{
  "name": "@hocx/blocklists",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && hostlist-compiler -c configuration.json -o dist/index.html -v"
  },
  "devDependencies": {
    "@adguard/hostlist-compiler": "^1.0.24"
  }
}
